# A project by Brian Munger

import sys

# Set up mode from arguments if they exist, otherwise assume mode 0
mode = 0
if (len(sys.argv) > 1):
    mode = sys.argv[1]


# Mode 0 = Buy / Sell / Hold now
if (mode == 0):

elif (mode == 1):